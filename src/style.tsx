import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    width: 200,
    height: 200,
  },
  title: {
    fontSize: 14,
  },
});

export default useStyles;
